{
  "name": "emergencyphone",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && astro preview",
    "deploy:qa": "astro build && firebase deploy --only hosting:qa"
  },
  "devDependencies": {
    "astro": "^1.0.0-beta.57"
  },
  "dependencies": {
    "comlink": "^4.3.1",
    "firebase": "^9.8.4",
    "idb": "^7.0.2"
  }
}
